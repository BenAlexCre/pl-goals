# Project Board

Last reviewed: 2026-08-09.

This is the **active work tracker** — a Kanban view of what's queued, in progress,
blocked, or done. It's derived from, and must stay consistent with,
[roadmap.md](./roadmap.md) (the plan) and [current-state.md](./current-state.md)'s
issue register (the facts). `/checkpoint` is responsible for keeping this file
current every session: moving cards between columns, adding cards for newly
discovered issues, and archiving completed ones into [Done](#done) rather than
deleting them.

**Every card references an `ISSUE-N` id** where one exists, linking to
[current-state.md](./current-state.md) for full detail — this board only carries a
one-line summary and a column. Net-new feature work with no corresponding bug/gap
(nothing is "wrong," it just doesn't exist yet) doesn't get an `ISSUE-N` — see
[roadmap.md](./roadmap.md) items 19–20 for why that distinction is kept.

Column meanings:

| Column | Meaning |
|---|---|
| **Backlog** | Known and prioritized, but not next up |
| **Ready** | Unblocked, scoped, actionable right now |
| **In Progress** | Actively being worked this session |
| **Blocked** | Can't proceed without something external (usually: live Supabase access) |
| **Testing** | Implementation done, being verified |
| **Done** | Complete — kept here as a record, not deleted when superseded |

See also: [roadmap.md](./roadmap.md) (why things are prioritized this way),
[current-state.md](./current-state.md) (the issue register these cards point to).

## Backlog

- **Reconcile the two pick-building flows** — `ISSUE-7` (P1) → [current-state.md](./current-state.md#issue-7--two-pick-building-flows-enforce-different-eligibility-rules). Correctness resolved 2026-08-05 (both flows now enforce the same server-side rule via `submit-pick5-picks`); the remaining UI-list inconsistency (`PicksPage.jsx` doesn't filter goalkeepers from its player list) is now cosmetic only.
- **Finish or remove invite-code join flow** — `ISSUE-8` (P1) → [current-state.md](./current-state.md#issue-8--no-self-serve-pot-join-flow). Redemption RPC (`redeem_invite()`) is drafted in `004_game_engine_shared_platform.sql`, deliberately missing `max_members`/`status` checks until Milestone 7 gives it a UI caller.
- **Gate `/admin` behind a role check** — `ISSUE-9` (P1) → [current-state.md](./current-state.md#issue-9--admin-has-no-ui-level-role-gate). Now also applies to `/admin/payments` (2026-08-05) — any signed-in user can navigate there and see the pot-selector UI, though every actual write still requires the backend's own `admin-actions` authorization check (pot-admin or app-admin), which correctly rejects an unauthorized caller regardless of UI access.
- **Authenticate `compute-deadlines`/`compute-scores`/`settle-gameweek`** — `ISSUE-26` (P1) → [current-state.md](./current-state.md#issue-26--compute-deadlinescompute-scoressettle-gameweek-accept-unauthenticated-requests). Needs a product decision (mirror `admin-actions`' pattern: allow an app-admin session OR the service-role key) — a blind service-role-only gate would break `AdminDashboard.jsx`'s existing manual-trigger buttons.
- **Standardize on one data-fetching pattern** — `ISSUE-10` (P2) → [current-state.md](./current-state.md#issue-10--duplicated-data-fetching-pattern). `PotDetail.jsx` still uses its own imperative fetch style, not TanStack Query hooks — deliberately not converted during the Pick 5 frontend cutover (2026-08-05), since that's this issue's concern, not a schema dependency. Also the root cause of `ISSUE-27` (no stale-response guard across its 5 data-loading effects).
- **Add stale-response guards to `PotDetail.jsx`'s data-loading effects** — `ISSUE-27` (P2) → [current-state.md](./current-state.md#issue-27--potdetailjsxs-data-loading-effects-have-no-stale-response-guard). Likely folds into `ISSUE-10`'s eventual fix rather than a standalone patch.
- **Clean up redundant undocumented RLS policies** — `ISSUE-28` (P2) → [current-state.md](./current-state.md#issue-28--numerous-undocumented-redundant-rls-policies-not-captured-in-any-migration). Harmless (each is a subset of an already-documented policy), but real migration/live drift — needs per-policy verification before a bulk drop, not a hardening-sprint-sized fix.
- **Delete or finish dead code** (`entryBuilder.jsx`, `gameAPI.js`, `footballDataProvider.js`, `whoScored.js`) — `ISSUE-11` (P2) → [current-state.md](./current-state.md#issue-11--dead-code-including-a-latent-case-sensitivity-import-bug)
- **Consolidate football-data.org sync scripts** — `ISSUE-12` (P2) → [current-state.md](./current-state.md#issue-12--overlapping-unused-football-dataorg-sync-scripts)
- **Resolve duplicate `.env` files** — `ISSUE-13` (P2) → [current-state.md](./current-state.md#issue-13--duplicate-env-files)
- **Remove debug `console.log` in `useAuth.js`** — `ISSUE-18` (P2) → [current-state.md](./current-state.md#issue-18--useauthjs-logs-the-signed-in-users-email-to-the-browser-console)
- **Fix 31 pre-existing type errors in `sync-fixtures/index.ts`** — `ISSUE-38` (P2) → [current-state.md](./current-state.md#issue-38--sync-fixturesindexts-fails-deno-check-with-31-pre-existing-type-errors). Every Game-Engine-era Edge Function passes `deno check` cleanly; this legacy, never-refactored function does not. Not a runtime bug — flagged during Phase 7 Stage 1's baseline verification.
- **Automated tests** — `ISSUE-16` (P3) → [current-state.md](./current-state.md#issue-16--no-automated-tests). Partially addressed for new code going forward: the Game Engine framework has its own Deno test suite (Milestone 3); the original frontend/edge-function code this issue describes is still untested.
- **Notification delivery** (email/push/SMS beyond the in-app inbox; deadline reminders) — no issue id, net-new feature → the in-app domain-event write is implemented for all three modes ([game-engine.md § GE-4.8](./game-engine.md#ge-48-notifications)); an actual delivery mechanism beyond that is still undesigned, deferred to Milestone 7 — see [decisions.md § Notifications](./decisions.md#notifications-domain-events-not-delivery). Distinct from `ISSUE-37` (Ready, below), which covers the simpler in-app inbox UI for the rows that already exist.
- **Avatar upload** — no issue id, net-new feature → [roadmap.md § P3](./roadmap.md#p3--known-product-gaps-unbuilt-not-broken)

## Ready

- **`ISSUE-33` — Last Man Standing and Score Predictor have zero frontend integration** (P1) → [current-state.md](./current-state.md#issue-33--last-man-standing-and-score-predictor-have-zero-frontend-integration). The single largest gap found by the Phase 7 Stage 1 audit (2026-08-09): both modes' full 8-method backends exist and are verified, but no page/hook creates an entry, submits a pick, or renders standings/notifications correctly for either mode. No longer blocked — `ISSUE-34` (the pot-creation prerequisite) resolved 2026-08-09 — now the top implementation priority.
- **`ISSUE-35` — Season-scoped (LMS/Predictor) payments have no admin UI path** (P1) → [current-state.md](./current-state.md#issue-35--season-scoped-lmspredictor-payments-have-no-admin-ui-path). `AdminPayments.jsx` is hard-scoped to `gameweek`-scoped payments only; the backend already supports season-scoped `mark_paid`/`mark_unpaid`.
- **`ISSUE-36` — `reinstate_entry` (Late Payment Override) has no UI trigger** (P1) → [current-state.md](./current-state.md#issue-36--reinstate_entry-late-payment-override-has-no-ui-trigger). Backend implemented and live-verified across all three modes 2026-08-08; zero frontend callers.
- **`ISSUE-37` — No notifications UI exists for any game mode** (P3) → [current-state.md](./current-state.md#issue-37--no-notifications-ui-exists-for-any-game-mode). Consolidates the three now-removed "no frontend UI for Predictor standings/notifications" notes below — the gap is platform-wide (Pick 5 included), not per-mode.
- **`ISSUE-39` — No gameweek in the seed data has `is_current = true`; the current season's Premier League has zero gameweeks** (P1) → [current-state.md](./current-state.md#issue-39--no-gameweek-anywhere-in-the-seed-data-has-is_current--true-the-current-seasons-premier-league-has-zero-gameweeks). Discovered live-verifying `ISSUE-34`'s fix — breaks `Dashboard.jsx`'s current-gameweek banner and makes the "— Current" league a dead end for any gameweek-dependent feature, including the new pot-creation form. Needs a product decision (which league/season's data should actually be marked current), not a silent SQL fix.
- **`season_end_tie_rule = final_prediction`** — needs a new pick-type table (winning team / first goalscorer / goal minute) and its own scoring logic. `LmsEngine.awardPrize()` throws `LmsFinalPredictionNotImplementedError` rather than guessing if a pot configured this way ever actually reaches a season-end tie. Only reachable at the very end of a competition; deliberately not designed ahead of the slice that needs it. Naming it will need to dodge the retired prototype's `predictor_picks` the same way `lms_team_picks` dodged `lms_picks` — see [game-engine.md § GE-15](./game-engine.md#ge-15-explicitly-deferred--not-carried-forward).
- **Activating a draft rollover pot** — `status` leaving `'draft'`. `createRolloverPot()` (Slice 8) creates the pot correctly but never activates it, by design; no Game Engine method currently transitions a pot's own `status`, so this needs its own design pass (likely an organiser-facing action, not automatic).
- **A rollover-specific notification** — telling the organiser their competition rolled over into a new draft pot. Considered during Slice 9, deliberately not built: no Pick 5 equivalent to model, and not explicitly asked for. See [decisions.md § LMS notifications](./decisions.md#lms-notifications).
- **Extract the `pot_standings_snapshots`/`pot_prizes` partial-index upsert/get-or-create-by-id workaround into a shared helper** — now duplicated privately in `Pick5Engine`, `LmsEngine`, *and* `PredictorEngine` (genuinely shared-platform-table mechanics, not mode-specific). `rankWithTies()` is also now duplicated a second time (Pick 5, Predictor). Low-risk cleanup, not urgent; flagged in [decisions.md § LMS standings](./decisions.md#lms-standings), [decisions.md § LMS prize awarding](./decisions.md#lms-prize-awarding), and [decisions.md § Score Predictor standings](./decisions.md#score-predictor-standings).
- **Score Predictor pick-reuse restriction** — `predictor_cycle_mode`'s "reuse restriction" is confirmed real by GE-5.3's own text, but neither which predictions it restricts (scoreline? goalscorer? both?) nor how "half" is computed for `two_halves` mode is specified anywhere reliable. `PredictorEngine.validateEntry()` (Slice 2) currently enforces no reuse restriction at all — a player may predict the same scoreline or the same goalscorer any number of times. Real, known gap, not silently omitted. See [decisions.md § Score Predictor pick submission](./decisions.md#score-predictor-pick-submission-slice-2).
- **Score Predictor payout model for `predictor_cycle_mode = 'two_halves'`** — does a `two_halves` pot pay out once (season end only) or twice (half-cycle boundary and season end)? Two existing pieces of evidence (the `pot_prizes` lazy-creation ADR's forward note vs. the retired prototype's own settlement functions) point in different directions; genuinely undecided. Confirmed, Slices 7-8, this does **not** block `determineWinner()`/`awardPrize()` as implemented — both compute/pay the season-end outcome identically regardless of cycle mode — but a *second*, earlier payout at the half-cycle boundary (if the product actually wants one) is a wholly separate, not-yet-designed invocation neither method has any part of. See [decisions.md § Score Predictor prize awarding](./decisions.md#score-predictor-prize-awarding).
- **Score Predictor entry-window rule** — genuinely undecided whether a mid-season joiner should be blocked (LMS's model) or allowed (Pick 5's model, though Predictor's cumulative scoring makes the fairness case weaker than Pick 5's per-gameweek-independent one). **Correction, 2026-08-09**: `pots.start_gameweek_id` is a shared column (added `013_lms_wipeout_and_rollover.sql`, not LMS-specific at the schema level) — it already exists and could be set on a Predictor pot today; the real gap is that `get-or-create-predictor-entry` has no `checkEntryWindow()`-equivalent reading it at all, unlike LMS's. No migration needed, only a `validateEntry()`/entry-creation decision. See [decisions.md § Score Predictor architecture review](./decisions.md#score-predictor-architecture-review).
- **Score Predictor: no pick-level void marker on `predictor_fixture_picks`** — unlike `pick5_picks.result`/`lms_team_picks.result`, there's no column recording that an individual pick belongs to a voided (unpaid) entry. Found and flagged during Slice 5's review, deliberately not added (no reader would exist yet). See [decisions.md § Score Predictor settlement](./decisions.md#score-predictor-settlement).
*(the three items formerly here — no UI for `reinstate_entry`, Predictor standings, Predictor notifications — are now covered by `ISSUE-36`, `ISSUE-33`, and `ISSUE-37` above, respectively, folded in during the Phase 7 Stage 1 audit, 2026-08-09.)*

## In Progress

*(nothing in progress — Phase 7 Stage 2's first slice, `ISSUE-34` (pot creation form), implemented and verified live 2026-08-09, moved to Done below. Next up per the recommended order: `ISSUE-33` (LMS/Predictor entry, pick submission, standings, notifications UI) — not yet started, awaiting the next Stage 2 slice)*

## Blocked

- **ISSUE-24 — undocumented SQL trigger overwrites `gameweeks.deadline_utc` with a conflicting 15-minute offset** → [current-state.md](./current-state.md#issue-24--an-undocumented-sql-trigger-recomputes-gameweeksdeadline_utc-with-a-conflicting-incorrect-offset). Discovered 2026-08-05, live-confirmed via direct reproduction. `supabase_admin`-owned, not in any migration — same out-of-band pattern as ISSUE-1/ISSUE-21. Blocked on: a decision on which offset (15 min or the documented 30 min) is actually correct before either removing the trigger or changing `compute-deadlines` to match.
- **ISSUE-20 — prototype tables have RLS disabled and full anonymous write access** → [current-state.md](./current-state.md#issue-20--prototype-tables-have-rls-disabled-and-full-anonymous-write-access). Narrowed 2026-08-03 — the new schema ships fully RLS-protected; the 7 original prototype tables remain exposed. Blocked on: Phase 8 of `deployment-checklist.md` (final removal), or a Dashboard/support RLS fix on the old tables directly.
- **`sync-live-events-every-5-min` cron job removal** — `supabase_admin`-owned, `postgres` cannot unschedule it. Low severity (no data/security impact). Folded into Track B/Phase 8 scope, not tracked separately.
- **Verify what (if anything) refreshes `player_fixture_goals`** — `ISSUE-3` (P0) → [current-state.md](./current-state.md#issue-3--player_fixture_goals-materialized-view-is-never-refreshed).
  Live-verified as never refreshed; moot in practice until ISSUE-19 is fixed (nothing reaches it to refresh).
- **Decide the live-events ingestion story** — `ISSUE-4` (P0) → [current-state.md](./current-state.md#issue-4--sync-live-events-edge-function-is-referenced-but-doesnt-exist).
  Blocked on: a product decision (rebuild `sync-live-events` on api-football vs. formalize the WhoScored scraper), separate from the ISSUE-19 config fix both cron variants also need.

## Testing

*(nothing in Testing — the LMS architecture work and `ISSUE-32` fix below are both fully verified and moved to Done)*

## Done

- **Phase 7, Stage 2, Slice 1 — Pot creation form (`ISSUE-34`)** — 2026-08-09, implemented and verified live (not committed). Rebuilt `components/pot/potManager.jsx`'s create-pot form with the full pot-contract field set (previously just name/league — 2 of ~20 configurable columns), sectioned as Basics / Game mode / Entry & Prize / mode-specific settings, the last conditionally rendered on the selected `game_type` (LMS: start/end gameweek, wipeout resolution, season-end tie rule; Predictor: end gameweek, cycle mode, scorer scope, three scoring point values; Pick 5: no extra section needed). Extended `hooks/usePots.js`'s previously-unused `useCreatePot` mutation to accept the full config rather than duplicating the insert logic inline — reuse over duplication, and it's no longer dead code. Client-side validation deliberately matches the DB's own check constraints exactly (fee type/amount consistency, non-negative Predictor points), no stricter/invented rule. `season_end_tie_rule = final_prediction` is shown but disabled, since `LmsEngine.awardPrize()` still deliberately throws for it — the UI can't set up a guaranteed future failure. `end_gameweek_id` required for both LMS and Predictor (confirmed by reading both engines' `classifyOutcome()` directly: without it, `determineWinner()` returns "still in progress" forever, not an error, and there's no pot-edit UI yet to fix the omission later); `start_gameweek_id` required for LMS specifically (`get-or-create-lms-entry`'s entry-window check rejects everyone without it). Dashboard and the pot list now show each pot's game mode. No backend/schema changes — every column, constraint, and client-INSERT grant already existed. Live-verified through the real browser (Playwright): created one Score Predictor pot and one Last Man Standing pot (the latter exercising both fee types' conditional fields), both confirmed correct via direct DB read; missing-required-field validation correctly blocked submission client-side. All test data (2 pots, 1 user) removed by exact ID, independently re-verified as zero residue. Found, not fixed (a data problem, not a code bug, see `ISSUE-39`): no gameweek in the seed data is marked `is_current`, and the "current" season's Premier League has zero gameweeks at all. See [current-state.md § ISSUE-34](./current-state.md#issue-34--pot-creation-form-exposes-only-2-of-20-configurable-pot-contract-columns) (now in Resolved issues).
- **Phase 7, Stage 1 — Frontend Completion & Launch Readiness audit** — 2026-08-09, investigation-only (no code changed), reviewed and pending prioritization. Full gap analysis comparing the frontend against every implemented backend capability across all three game modes: audited every page, hook, admin surface, and Edge Function; verified every organiser/player user journey against real code (not assumed wired). Headline finding: `ISSUE-33` — Last Man Standing and Score Predictor have **zero** frontend integration despite fully implemented, tested, live-verified 8-method backends (confirmed by an exhaustive grep across `frontend/src` for every LMS/Predictor-related term and for `game_type` itself — zero matches on all of them). Root cause identified as `ISSUE-34` — the pot-creation form sets only `name`/`league_id`/`season_id`, so an organiser cannot create a pot of any type other than default-configured Pick 5 in the first place. Four more new issues registered: `ISSUE-35` (season-scoped LMS/Predictor payments have no admin UI path), `ISSUE-36` (`reinstate_entry` has no UI trigger), `ISSUE-37` (no notifications UI for any mode, Pick 5 included). Three existing issues extended with newly confirmed facts: `ISSUE-26` (the same missing-auth-check gap also applies to `sync-fixtures`), `ISSUE-8` (confirmed `remove_member` has the identical no-UI gap as `add_member`; corrected a stale cross-reference to the now-resolved `ISSUE-6`), `ISSUE-11` (five more confirmed-dead exports/components: `usePick5Entry.js`, `useAdminAction`, `usePot`/`useCreatePot`, `MemberTable.jsx`, `LeaderboardCard.jsx`, plus an orphaned route, `/pot/:potId/picks`). No backend bugs found beyond what was already known — the three previously-documented unauthenticated Edge Functions (`compute-deadlines`/`compute-scores`/`settle-gameweek`) were re-confirmed still open, unchanged. Per the task's own explicit instruction, no frontend/admin code was written this stage — the audit, gap matrix, and a recommended implementation order were delivered for review before any Stage 2 implementation begins. See [current-state.md](./current-state.md) for the full per-issue detail and [session-log.md](./session-log.md) for the complete audit report.
- **Milestone 6, Slice 9 — Score Predictor notifications** — 2026-08-09, implemented and verified live (not committed). Reviewed `Pick5Engine.notifyUsers()`/`LmsEngine.notifyUsers()` first — confirmed the two existing implementations are byte-for-byte identical to each other, leaving no genuine design-decision surface for a third copy to diverge on: insert one `notifications` row, throw on error. `PredictorEngine.notifyUsers()` implemented as the same shape. New event type `predictor.prize_awarded` — only one event exists, since `awardPrize()` has exactly one non-trivial outcome shape (`season_end`). Payload `{ amount, tied }` — `tied: winners.length > 1` is the Predictor-specific analog to Pick5's `gameweekId`/LMS's `outcome`, chosen because neither existing field would carry real information for Predictor's single-outcome, non-gameweek-scoped shape. One notification per winning user, called from `awardPrize()`'s recipient loop after the trailing `pot_prizes` write, each wrapped in its own try/catch that logs and never propagates — settlement and payout are already durably written by the time a notification is even attempted. Idempotency comes free from `awardPrize()`'s own existing `pot_prizes.is_settled` short-circuit — no new dedup mechanism needed, consistent with the "do not redesign notifications" instruction. No schema change. 7 new unit tests (312/312 across `supabase/functions/`). Live-verified through the real `settle-gameweek`/`compute-scores` Edge Functions: a sole winner received exactly one notification (`tied:false`), a genuine tie produced exactly two (`tied:true` each), a repeated call left notification counts unchanged (idempotency), and a third pot proved failure isolation via a real, client-side-intercepted write failure — the prize and payout were still written in full and zero notification rows existed — 15 checks, all passing; all test data removed by exact ID, independently re-verified as zero residue. **All eight `GameEngine` contract methods are now implemented for Score Predictor — Milestone 6's core implementation work is complete.** See [decisions.md § Score Predictor notifications](./decisions.md#score-predictor-notifications).
- **Milestone 6, Slice 8 — Score Predictor prize awarding + Pick 5 tiebreak correction** — 2026-08-08/09, implemented and verified live (not committed). A stated "Pick 5" product rule (winner hierarchy: points, then exact score count, then goalscorer count, then split) was checked against Pick 5's actual data model before implementing anything — its vocabulary matched nothing in Pick 5's own picks (`pick5_picks.goal_threshold`/`goals_scored`), only Score Predictor's own scoring columns — and raised directly with the repo owner per the task's own "stop and ask, don't invent" instruction, rather than guessed at. **Confirmed: the rule was meant for Score Predictor.** Pick 5's `determineWinner()`/`awardPrize()` are entirely unchanged this slice (confirmed via `git diff` — zero changes to `pick5/engine.ts` — and its full, untouched test suite passing as the regression check). `PredictorEngine`'s `classifyOutcome()` (Slice 7) now applies the hierarchy to its `season_end` winner set, revising Slice 7's own correct-at-the-time "no secondary tiebreak" conclusion; deliberately not applied to `generateStandings()`'s own ranking. `PredictorEngine.awardPrize()` implemented: one `pot_prizes` row (`scope='season'`, LMS's single-conclusion shape, not Pick 5's weekly one); calls `determineWinner()` directly (matches Pick 5's own choice, not LMS's classifyOutcome()-direct one — no richer type to lose); deductions identical to Pick 5/LMS minus LMS's rollover-only carry-over term; zero winners is a silent no-op (LMS's philosophy); `pot_prizes` written last from the start; new `PredictorPrizePoolExceededError` mirrors both existing modes'. `settle()`'s per-pot loop now also calls `awardPrize()`. No schema change. 13 new unit tests for `awardPrize()` (including a dedicated fake-level failure-injection flag proving retry-safety) plus 3 revised/new tiebreak tests for `determineWinner()` — 305/305 across `supabase/functions/`. Live-verified through the real `settle-gameweek`/`compute-scores` Edge Functions: a sole winner with percentage + fixed fee deductions received the full net prize while a non-winning but still-settled entry received nothing; a genuine complete tie split evenly; a second real call left every payout/prize row unchanged; a third pot proved retry-safety against a real, client-side-intercepted write failure with zero persistent database mutation — 24 checks, all passing; all test data removed by exact ID, independently re-verified as zero residue. See [decisions.md § Score Predictor prize awarding](./decisions.md#score-predictor-prize-awarding).
- **Milestone 6, Slice 7 — Score Predictor winner determination** — 2026-08-08, implemented and verified live (not committed). Reviewed `Pick5Engine.determineWinner()`/`LmsEngine.determineWinner()` first; justified rather than assumed. `PredictorEngine.determineWinner()` recomputes directly from `game_entries`/`game_entry_predictor` — never `pot_standings_snapshots` — per the explicit "never depend on cached state" requirement, matching `LmsEngine`'s own choice, not `Pick5Engine`'s (whose snapshot read is only safe because a settled Pick 5 gameweek can never change retroactively). Only one conclusion path (`pots.end_gameweek_id`'s deadline passed), unlike LMS's four, since Predictor has no elimination concept to conclude the competition early. Confirmed, not assumed, that `predictor_cycle_mode` has no bearing on this method — the fixed `(ctx, potId)` signature can only ever answer the season-end question, and that computation doesn't change based on cycle mode; the still-open "does `two_halves` need a separate half-cycle determination" question remains exactly as unresolved as before (see Ready, above). Every entry tied for the highest cumulative score wins; void entries excluded; a reinstated entry included automatically, no special-case code. Purely read-only — no writes, no pot_prizes, no notifications. A private `classifyOutcome()` helper mirrors `LmsEngine`'s own split, reusable by a future `awardPrize()` slice. No schema change. 9 new unit tests (296/296 across `supabase/functions/`). Live-verified by calling the real, shipped `PredictorEngine` class directly against real database state produced entirely through real Edge Function calls (`compute-scores`, `admin-actions`) — not through an HTTP endpoint, since this method isn't wired into any Edge Function yet, the same standalone shape Pick5's/LMS's own Slice 7 had: two tied winners correctly identified, a void entry correctly excluded despite matching their score, the same entry correctly joining the tie once reinstated via the real `admin-actions` Edge Function, repeated calls returning identical results with zero writes, and a single-entry pot correctly producing exactly one winner — 13 checks, all passing; all test data removed by exact ID, independently re-verified as zero residue. See [decisions.md § Score Predictor winner determination](./decisions.md#score-predictor-winner-determination).
- **Milestone 6, Slice 6 — Score Predictor standings** — 2026-08-08, implemented and verified live (not committed). Prerequisite investigation first (per explicit instruction): confirmed `LmsEngine.generateStandings()`'s previously-flagged read-side gap was real — no `game_entries.status` filter at all, so a voided entry's stale `competitive_status` could still render as "alive." Fixed with the smallest correction (`.neq('status', 'void')`), same idempotent full-recompute method, no redesign — see [decisions.md § LMS standings must exclude voided entries](./decisions.md#lms-standings-must-exclude-voided-entries). Reviewed `Pick5Engine.generateStandings()`/`LmsEngine.generateStandings()` for the main objective; justified rather than assumed — `PredictorEngine.generateStandings()` ranks like Pick 5 (`rankWithTies()`, a real cumulative score, duplicated per GE-18) but is shaped like LMS (overall row only, no per-gameweek payout concept exists for Predictor). Void entries excluded with the identical corrected filter; `meta` holds `{ exactScoreCount, correctScorerCount }`; no Predictor-specific tiebreak invented. `settle()` restructured (not just appended to) so standings refresh unconditionally per pot every tick, not only when voiding happens — the same revision Pick5's/LMS's own `settle()` needed when each shipped generateStandings() for the first time. Confirmed a reinstated entry reappears automatically, with zero special-case code, since the method has no memory of any previous snapshot. No schema change. 13 new unit tests for Predictor + 2 for the LMS prerequisite fix (287/287 across `supabase/functions/`). Live-verified through the real `compute-scores` and `admin-actions` Edge Functions: five real entries (exact score, two tied correct-results, a wrong result, and a reinstated voided entry) produced the correct tie-sharing rank shape and correct `meta`, and a repeated `reinstate_entry` call left every score/rank unchanged — 15 checks, all passing; all test data removed by exact ID, independently re-verified as zero residue. See [decisions.md § Score Predictor standings](./decisions.md#score-predictor-standings).
- **Prerequisite correction — Late Payment Override (`reinstate_entry`, all three modes)** — 2026-08-08, implemented and verified live (not committed). Reviewed the payment lifecycle across Pick 5/LMS/Predictor, all three `settle()` implementations, `admin-actions`, and `generateStandings()` before designing anything, per explicit instruction. New business rule: an admin may explicitly accept a late payment and separately, explicitly reinstate a voided entry — never automatic. Determined a bare `status='pending'` flip is insufficient (once a gameweek is `'completed'`, `compute-scores` never revisits it, so a voided entry's missed gameweeks would never get scored on their own) — reinstatement instead reuses the existing `calculateScore()`/`settle()` GameEngine methods directly (one gameweek for Pick 5's gameweek-scoped entry; every gameweek in the pot's season, ascending, for LMS/Predictor's season-scoped entry), no scoring logic reimplemented. A settled `pot_prizes` row for the entry's own competition instance (gameweek-scoped for Pick 5, season-scoped for LMS/Predictor) hard-blocks reinstatement — money already paid out is never reopened; confirmed Pick 5 needs this feature too (its weekly settlement cadence actually makes the guard trigger *more* often, not less). Lives in `admin-actions/reinstate.ts` as a new `reinstate_entry` action, not a 9th `GameEngine` method — shared, cross-mode admin tooling, same category as `mark_paid`/`mark_unpaid`. Pure decision logic (`decideReinstatement()`) split from DB orchestration, mirroring `bulkPayments.ts`'s own established pattern. Schema: two new `game_entries` columns, `reinstated_at`/`reinstated_by` (audit trail; no client grant, verified live). **Found and fixed a real, blocking, pre-existing bug during the required admin-actions review**: `mark_paid`/`mark_unpaid`'s upsert never actually worked for season-scoped (LMS/Predictor) payments past the first write — confirmed live (a second write hard-failed) — corrected GE-4.3's own prior unverified claim that no Payment Verification changes were needed for LMS. Fixed with the same get-or-create-by-id workaround already used for `pot_standings_snapshots`/`pot_prizes`. 12 new unit tests (274/274 across `supabase/functions/`). Live-verified through the real `admin-actions` Edge Function: non-admin forbidden (403); reinstate-before-payment rejected; the season-scope upsert fix exercised directly (a second write, previously fatal, now succeeds); a manually-seeded settled `pot_prizes` row blocks reinstatement; a real LMS reinstatement correctly re-resolved a previously-void pick and ran the pot through to a genuine paid conclusion; a further call on that now-concluded pot was correctly blocked, unprompted; a never-voided entry is a harmless no-op; a real Score Predictor reinstatement correctly re-resolved an unpaid entry's pick and updated its cumulative totals — 18 checks, all passing; all test data removed by exact ID, independently re-verified as zero residue. See [decisions.md § Late Payment Override](./decisions.md#late-payment-override).
- **Cross-slice correction — `calculateScore()` must not mutate a voided entry (LMS + Score Predictor)** — 2026-08-08, implemented and verified live (not committed). Investigated per explicit instruction, ahead of Slice 6, whether scoring could still mutate a voided entry after settlement — confirmed real, live-reachable bugs in both modes, not hypothetical. LMS: `calculateScore()` selects entries via `game_entry_lms.competitive_status = 'alive'`, a column `settle()`'s void step never touches, so a voided entry stays `'alive'` indefinitely and gets incorrectly eliminated (missing-pick branch) or has an already-void pick's result overwritten by a later gameweek's run. Score Predictor: `calculateScore()` had no `game_entries.status` awareness at all (the exact gap flagged during Slice 5's own review), so a voided entry's unresolved pick could still be scored and folded into `game_entry_predictor`'s totals. Pick 5 confirmed unaffected (its own `calculateScore()` already filters `status = 'locked'`). Smallest shared correction: both engines' `calculateScore()` now additionally filter `game_entries` to `status = 'pending'` — one line each, no schema change, `settle()` itself untouched in both modes. Found and flagged (not fixed, out of this fix's scope) a related read-side sibling bug: `LmsEngine.generateStandings()` has the identical missing filter (see Ready, above). 4 new unit tests (262/262 across `supabase/functions/`, after fixing 11 tests that broke because the test fakes' default `game_entries` fixture had no `status` field). Live-verified through the real `compute-scores` Edge Function: a real voided LMS entry with a stale-`'alive'` extension row and no pick, and a real voided Predictor entry with one unresolved pick against a real finished fixture — one real call confirmed neither was touched, 3 checks, all passing; all test data removed by exact ID, independently re-verified as zero residue. See [decisions.md § calculateScore() must not mutate a voided entry](./decisions.md#calculatescore-must-not-mutate-a-voided-entry).
- **Milestone 6, Slice 5 — Score Predictor settlement** — 2026-08-08, implemented and verified live (not committed). Reviewed `Pick5Engine.settle()`/`LmsEngine.settle()` first; justified rather than copied — `PredictorEngine.settle()` mirrors LMS's own "deliberately small" Slice 5 shape (payment-void rule only, no standings/determineWinner()/awardPrize()/notifications, per explicit instruction). Resolved five product questions posed up front: settlement has no independent cycle/season boundary concept (gated only by the caller's own "fixtures all finished" check); the payment-void check runs every gameweek, not once per cycle/season, since a flat one-time fee has no cycle-dependent timing; `predictor_cycle_mode` has no bearing on this method at all, confirmed by review; unpaid entries flip `game_entries.status = 'void'`, already-computed points are left untouched (exclusion from any ranked output deferred to `generateStandings()`, a future slice); safe to rerun indefinitely, proven by test and by a real second live call. Confirmed, resolving GE-4.3's own "still undecided" hedge, that `entry_payments.scope = 'season'` (a flat, one-time competition fee) is Predictor's correct payment shape — structurally forced by its season-scoped `game_entries`, same as LMS. Found and flagged two real gaps rather than silently fixing or ignoring them (see Ready, above): no pick-level void marker on `predictor_fixture_picks`, and no `game_entries.status` awareness in `calculateScore()` (a narrower, previously-unflagged version of the same shape already exists in `LmsEngine`). No schema change. `settle-gameweek` needed only a missing `predictor/index.ts` registration import, no discovery bug this time. 10 new unit tests (258/258 across `supabase/functions/`). Live-verified through the real `settle-gameweek` Edge Function: a real gameweek (9) with its one fixture temporarily flipped to `'finished'` (settle() itself never reads fixture data — only the caller's own readiness gate needed it), a paid and an unpaid entry in a real pot — 10 checks, all passing, including the entry voiding, the gameweek's own `'completed'` side effect, and idempotency across two real calls after reopening the gameweek; all test data removed by exact ID and both temporarily-changed statuses reverted to their originals, independently re-verified, separate from the script's own cleanup report. See [decisions.md § Score Predictor settlement](./decisions.md#score-predictor-settlement).
- **Milestone 6, Slice 4 — Score Predictor scoring** — 2026-08-08, implemented and verified live (not committed). Reviewed `Pick5Engine.calculateScore()`/`LmsEngine.calculateScore()` first; justified rather than copied. Resolved five product questions posed up front (draw representation — reuses Slice 2's equal-scoreline shape, no new column; no interim pre-finish state beyond `points_awarded IS NULL`; a missing goalscorer prediction simply never matches, no penalty; postponed/cancelled fixtures left unresolved, same conclusion `LmsEngine.calculateScore()` reached independently for its own not-yet-finished case; safe to rerun indefinitely, proven by test and live double-call) plus a sixth raised mid-review: the repo owner rejected a fixed-value framing for the scorer bonus (offered 1/2/3 points via `AskUserQuestion`) and asked instead for all three point values to be organiser-configurable per pot, defaulting to 5/3/2. `019_predictor_scoring_config.sql` applied — three new `pots` columns (`predictor_exact_score_points`/`predictor_correct_result_points`/`predictor_scorer_bonus_points`), immutable once the pot has entries (same rule as `predictor_cycle_mode`/`predictor_scorer_scope`); two new `predictor_fixture_picks` columns (`is_exact_score`/`scorer_bonus_awarded`) give `game_entry_predictor`'s aggregates an unambiguous source of truth independent of the now-configurable point values, since a correct-result-plus-bonus total could otherwise collide with an exact-score total under some pot's own configuration. `PredictorEngine.calculateScore()` is the first `calculateScore()` among the three modes that reads `pots` directly. `game_entry_predictor`'s cumulative stats are a full SUM/COUNT recompute every call (all gameweeks), never an increment — same discipline `LmsEngine.generateStandings()` established for a season-scoped aggregate. `compute-scores` needed only a missing `predictor/index.ts` registration import, one line, no discovery bug this time (the dispatch loop was already fully generic). 15 new unit tests (248/248 across `supabase/functions/`). Live-verified through the real `compute-scores` Edge Function: a real, already-finished fixture (gameweek 2, a genuine 4-1 result) with a real goal event seeded into `fixture_events` and `player_fixture_goals` refreshed, three real entries (exact score + correct goalscorer, correct result only, wrong result) — 10 checks, all passing, including the goalscorer bonus and idempotency across two real calls; all test data (1 pot, 3 users, 1 seeded `fixture_events` row) removed by exact ID, re-verified as zero rows across every table touched. See [decisions.md § Score Predictor scoring](./decisions.md#score-predictor-scoring).
- **Milestone 6, Slice 3 — Score Predictor locking** — 2026-08-08, implemented and verified live (not committed). Reviewed Pick 5's and LMS's `lockEntries()` first; justified rather than assumed. Locks the individual prediction (`predictor_fixture_picks.locked_at`, new column, `018_predictor_fixture_picks_locked_at.sql`), not the season-scoped entry — the same conclusion LMS reached, independently derived from the same structural fact (season-scoped `game_entries`), not copied; "lock both" considered and rejected (no concept, at this level, of the entry needing to become non-submittable — that's a future settlement concern). No pot-id filter needed, same reasoning as `LmsEngine.lockEntries()` — `predictor_fixture_picks` is written only by an already-gated function. `validateEntry()` needed no changes, confirmed not assumed — the live deadline check it already had is strictly at least as current as `locked_at` can ever be. No LMS-style shared-scheduler discovery bug this time — `compute-deadlines`'s dispatch loop was already fully generic (fixed for LMS in Milestone 5 Slice 3); the only gap was a missing `predictor/index.ts` registration import, one line, zero mode-specific branching added. 4 new unit tests (233/233 across `supabase/functions/`). Live-verified through the real `compute-deadlines` Edge Function against two real gameweeks (one past-deadline, one not-yet-due): 7 checks, all passing, including idempotency across two real calls; all test data removed by exact ID, re-verified as zero rows across every table touched. See [decisions.md § Score Predictor locking](./decisions.md#score-predictor-locking).
- **Milestone 6, Slice 2 — Score Predictor pick submission** — 2026-08-06, implemented and verified live (not committed). Focused architecture review first, re-reading GE-5.3's exact text rather than relying on the Slice 1 review's memory — confirmed `predictor_cycle_mode`'s reuse restriction is real (not purely prototype-inferred) but still underspecified. Of the five open questions: two resolved by design (a draw is an equal predicted scoreline, no separate column — derived from GE-5.3's own mutual-exclusivity scoring text; goalscorer optional — asked the repo owner directly, confirmed by `AskUserQuestion` rather than assumed); two confirmed not to block this slice (scorer bonus point value, entry-window rule); one partially deferred (reuse restriction — shipped without enforcing it, flagged as a real gap, not guessed at). `017_predictor_picks.sql` applied: `predictor_fixture_picks` table, named to dodge the retired prototype's own `predictor_picks` collision (confirmed live before writing the migration). `PredictorEngine` class created; `validateEntry()` implemented (entry status, live deadline, fixture-belongs-to-gameweek, goalscorer eligibility when provided) — no elimination/competitive-status check at all, confirmed by `game_entry_predictor`'s own schema having no such column. `submit-predictor-picks` implemented, with the malformed-JSON `.catch()` guard built in from the start. 25 new unit tests (229/229 across `supabase/functions/`). Live-verified through the real Edge Function over HTTP: 16 checks (auth, malformed JSON, missing fields, non-owner, wrong pot type, fixture/gameweek mismatch, ineligible goalscorer, valid draw submission, idempotent resubmission updating the same row, deadline-passed rejection), all passing; all test data removed by exact ID, re-verified as zero rows. See [decisions.md § Score Predictor pick submission](./decisions.md#score-predictor-pick-submission-slice-2).
- **Production Hardening Sprint** — 2026-08-06, implemented and verified (not committed). P0: the 7 `supabase_admin`-owned prototype tables (`ISSUE-20`) still had RLS fully disabled with unrestricted anonymous write access — re-confirmed live, re-confirmed the `ISSUE-21` ownership blocker empirically (`postgres` cannot alter or revoke on these), fixed in local dev via direct `supabase_admin` access (not available on a real hosted project — that needs the same SQL run by someone with equivalent access; tested SQL recorded in `current-state.md`). P1: malformed-JSON crashes in 6 Edge Functions fixed (`.catch(() => ({}))`, matching `settle-gameweek`'s existing pattern) — verified live, all now return clean 400s instead of bare 500s. P1: found and fixed an orphaned, always-failing duplicate cron job (`sync-live-events-every-5-min`) — `006_fix_cron_job_headers.sql` already correctly unschedules it, but the call silently failed (masked by its own broad exception handler) because the job is `supabase_admin`-owned, not `postgres`-owned — a genuinely new extension of `ISSUE-21`'s scope to `cron.job` rows. Several P2 findings documented only (a narrow LMS pick-submission race window, dead-code writes to the legacy `user_entries` table, unindexed FK columns with no evidence of an actual slow query) — see `current-state.md` and `session-log.md`. 204/204 tests pass, no regressions (every fix was additive). Live-verified: all 6 functions' malformed-JSON handling, the RLS fix's authenticated-read/anonymous-deny behavior, the cron job removal.
- **Milestone 6, Slice 1 — Score Predictor entry creation** — 2026-08-06, implemented and verified live (not committed). Architecture review done first (per explicit instruction): compared all eight `GameEngine` methods against Pick 5/LMS, reviewed the existing schema, and flagged five genuine, undecided product questions (draw representation, mandatory-or-optional goalscorer, scorer bonus point value, `two_halves` payout semantics, entry-window rule) rather than inventing answers — see [decisions.md § Score Predictor architecture review](./decisions.md#score-predictor-architecture-review). `get-or-create-predictor-entry` implemented, mirroring `get-or-create-lms-entry`'s season-scoped shape exactly, deliberately without an entry-window check (Predictor pots have no column to check against yet, and whether one's needed is itself open). No `PredictorEngine` class yet, matching when Pick 5's/LMS's own Game Engine classes first appeared (Slice 2). No schema change — `game_entries`/`game_entry_predictor` already existed; no migration drafted, since the one real schema need (a picks table) is blocked on the open questions above. 4 new unit tests (204/204 across the whole `supabase/functions/` tree). Live-verified through the real Edge Function over HTTP: missing `pot_id`, missing auth, wrong-game-type-pot, non-member all correctly rejected; entry creation and idempotency confirmed (same entry id, exactly one row) — 9 checks, all passing; all test data removed by exact ID, re-verified as zero rows.
- **Hardening sprint — `settle()` partial-write risk (both engines) and `Pick5Engine.awardPrize()` transactionality** — 2026-08-06, implemented and verified live. **Correction:** this entry previously said "committed and pushed" — `git log`/`git reflog` confirm that never actually happened; these changes remained uncommitted working-tree edits through the Milestone 6 kickoff and this Production Hardening Sprint. Flagged to the repo owner when discovered; not committed by this session either, per its own explicit instruction. `Pick5Engine.settle()`/`LmsEngine.settle()` both reordered to void picks before voiding the entry itself, closing a real retry-safety gap the architecture review found (an entry flipped to `void` before its picks were voided could never be found again by a retry if the picks write then failed). `Pick5Engine.awardPrize()` received the identical transactionality correction already applied to `LmsEngine.awardPrize()` — the `pot_prizes` write now runs last, after the payout loop. Also fixed, found while making that exact correction: the `pot_prizes` update/insert error handlers were throwing `Pick5PrizePoolExceededError` for any write failure, not just the fee-exceeds-gross case that error class actually means. Corrected the stale "DESIGN ONLY — NOT APPLIED" header comments on `010_prize_pool_deductions.sql`/`013_lms_wipeout_and_rollover.sql` (both confirmed applied; comment-only change, no SQL touched). 6 new failure-injection unit tests (148/148 across `game-engine/`). Live-verified: three partial-failure states seeded directly and confirmed retry-safe (6 checks, all passing); all test data removed by exact ID.
- **Milestone 5, Slice 9 — LMS notifications, plus a transactionality correction to Slice 8** — 2026-08-06, implemented and verified live (not committed). Before writing any Slice 9 code, verified whether `awardPrize()`'s rollover sequence (`pot_prizes` write, new pot creation, organiser membership) was fully transactional, per the repo owner's explicit question — it wasn't: `pot_prizes.is_settled = true` was written first, so any later failure (entry settlement, payout, or `createRolloverPot()` itself) left the pot permanently, incorrectly "concluded" with the rest of the work undone and unretryable. Fixed with the smallest correction available: moved the `pot_prizes` write to run last (settling/paying entries are naturally idempotent, safe to repeat), and gave `createRolloverPot()` its own idempotency guard (reuses an existing pot whose `rollover_source_pot_id` already matches, rather than creating a duplicate on retry). `LmsEngine.notifyUsers()` implemented as a one-for-one reuse of `Pick5Engine.notifyUsers()`'s architecture — same `notifications` table shape, same best-effort try/catch call-site pattern. One event type, `lms.prize_awarded`, fired per actual payout recipient from `awardPrize()` *after* its trailing `pot_prizes` write (not from inside the payout loop, unlike Pick 5 — keeps the "money and settlement record both already durable" invariant given LMS's own write order). No notification for a `roll_prize` wipeout. A rollover-specific notification was considered and deliberately not built. **All eight `GameEngine` methods are now implemented for LMS — Milestone 5's core implementation work is complete.** No schema change. 9 new unit tests (2 transactionality, 7 notifications — 70/70 in file, 142/142 across `game-engine/`). Live-verified: a simulated partial-failure retry state (rollover pot + membership pre-existing, no `pot_prizes` row) resolved correctly with no duplicates (5 checks); `notifyUsers()` standalone and wired through a single-survivor payout and a `roll_prize` wipeout (7 checks) — 12 checks total, all passing; all test data cleaned up by exact ID, re-verified as zero rows. See [decisions.md § LMS prize awarding: transactionality correction](./decisions.md#lms-prize-awarding-transactionality-correction) and [decisions.md § LMS notifications](./decisions.md#lms-notifications).
- **Milestone 5, Slice 8 — LMS prize awarding** — 2026-08-06, implemented and verified live (not committed). `LmsEngine.awardPrize()` designed against Pick 5's version but genuinely diverged everywhere the outcome model differs: single survivor gets the full net prize; wipeout `split_prize` splits equally among the group; wipeout `roll_prize` pays nobody and `createRolloverPot()` automatically creates the new draft pot (config copied, `rollover_source_pot_id`/`carry_over_amount`/`rollover_generation` set, name derived by stripping/re-appending `"(Rollover #N)"`, sole organiser member, never activated); season-end tie `split_prize` splits equally among still-alive entries; season-end tie `final_prediction` throws `LmsFinalPredictionNotImplementedError` rather than guessing, deliberately not built this slice; still-in-progress is a silent no-op (unlike Pick 5's `Pick5NoEligibleWinnersError` — normal, common state here since `awardPrize()` is now called every gameweek from `settle()`). Extracted `classifyOutcome()` (a behavior-preserving refactor of Slice 7's `determineWinner()` body) so both methods share one typed classification instead of re-deriving it from a flattened `string[]`. Closed the Slice 7 sequencing gap — `getEligibleLmsPotIds()` now excludes any pot with a settled `pot_prizes` row, so a concluded competition's `calculateScore()`/`settle()` calls stop reaching it. No schema change — every column used already existed from `013_lms_wipeout_and_rollover.sql`. 13 new unit tests (61/61 in file, 133/133 across `game-engine/`). Live-verified against the real database: single survivor (plus idempotency and the sequencing-gap check), wipeout + Split Prize, wipeout + Roll Prize (rollover pot fields/membership/draft status all confirmed), season-end tie + Split Prize (concluded and not-yet-concluded), still-in-progress — 27 checks, all passing; 6 pots and 12 users cleaned up by exact ID, re-verified as zero rows. See [decisions.md § LMS prize awarding](./decisions.md#lms-prize-awarding).
- **Milestone 5, Slice 7 — LMS winner determination** — 2026-08-06, implemented and verified live (not committed). `LmsEngine.determineWinner()` designed from scratch, not modeled on Pick 5's (a one-line "rank 1 of the most recent gameweek" lookup that assumes every settlement concludes an instance — false for LMS). Distinguishes one survivor (`[userId]`), a wipeout (zero alive, returns the group sharing the most recent `eliminated_gameweek_id`), a season-end tie (multiple alive, `pots.end_gameweek_id`'s deadline has passed, returns every alive entry), and still-in-progress (`[]`). Purely read-only — no elimination, `pot_prizes`, or rollover writes. Never reads `wipeout_resolution`/`season_end_tie_rule` itself, keeping Wipeout Resolution and Season End Resolution genuinely separate, per explicit instruction. Not wired into `settle()` yet, mirroring Pick 5's own Slice 7. Identified, not fixed, a real sequencing gap (nothing stops `calculateScore()` scoring a lone survivor again in a later gameweek). No schema change — reused `pots.end_gameweek_id` (existing since Milestone 2, previously unused by any mode) and the same live deadline-check pattern established in earlier slices. 9 new unit tests (172/172 total pass). Live-verified against the real database (no HTTP endpoint exists for this method yet, same as Pick 5's own Slice 7): all five scenarios (single survivor, wipeout, in-progress, season-end tie, not-yet-concluded) correct. See [decisions.md § LMS winner determination](./decisions.md#lms-winner-determination).
- **Milestone 5, Slice 6 — LMS standings** — 2026-08-06, committed and pushed. `LmsEngine.generateStandings()` designed from scratch, not modeled on Pick 5's points-based ranking (no points exist for LMS): every alive entrant ties for rank 1; eliminated entrants rank below, ordered by elimination recency (`eliminated_gameweek_id` descending), with same-gameweek eliminations (including a wipeout) tied; `score` is a plain 1 (alive) / 0 (eliminated); the elimination gameweek lives in `meta` — the first real use of that column anywhere in the codebase. Writes only the overall snapshot row (`gameweek_id = null`) — no meaningful per-gameweek row exists for a mode with no weekly-resetting score. Wired into `settle()` with the same per-pot failure isolation `Pick5Engine.settle()` established, revising Slice 5's own reasoning for this one harmless, idempotent method specifically. No schema change — `pot_standings_snapshots.meta` already existed. 7 new unit tests (163/163 total pass). Live-verified through the real `settle-gameweek` function across two gameweeks with five entries: alive-tied-at-1, elimination-recency ordering, and same-gameweek ties all confirmed. See [decisions.md § LMS standings](./decisions.md#lms-standings).
- **Milestone 5, Slice 5 — LMS settlement** — 2026-08-06, committed and pushed. `LmsEngine.settle()` implements the payment-void rule only (`entry_payments.scope = 'season'`), deliberately not duplicating `calculateScore()`'s already-done elimination work — never transitions `game_entries.status` to `'settled'` and never calls `generateStandings()`/`determineWinner()`/`awardPrize()`, since none of those conclude on an ordinary gameweek for a season-long LMS competition. Voiding an unpaid entry voids every one of its picks, across every gameweek, since payment covers the whole competition. Fixed the identical `game_entries.gameweek_id` discovery bug in `settle-gameweek` that Slices 3/4 already fixed elsewhere. No schema change. 6 new unit tests (156/156 total pass). Live-verified through the real `settle-gameweek` function with two entries in one pot: a verified `scope='season'` payment (stayed pending, pick untouched), no `entry_payments` row at all (voided, pick marked void — confirming a missing row is treated as unpaid). See [decisions.md § LMS settlement](./decisions.md#lms-settlement).
- **Milestone 5, Slice 4 — LMS scoring and elimination** — 2026-08-06, committed and pushed. `LmsEngine.calculateScore()` resolves each pick against its team's own fixture (`'winning'`/`'losing'` while live, `'won'`/`'lost'` once finished — `pick_result` has no `'drew'` value, a draw reuses `'lost'`, same elimination consequence as a loss) and eliminates the entry once that fixture is finished and not won. Per the repo owner's 2026-08-06 decision, a missed pick eliminates identically — no grace period, no automatic pick — found via a second, independent pass over still-alive entries with no pick row for the gameweek. Fixed the identical `game_entries.gameweek_id` discovery bug in `compute-scores` that Slice 3 already fixed in `compute-deadlines`. No schema change — reused `game_entry_lms.competitive_status`/`eliminated_gameweek_id`, `lms_team_picks.result`, `pots.start_gameweek_id`, all pre-existing. 10 new unit tests (150/150 total pass). Live-verified through the real `compute-scores` function with four entries in one pot: won (alive), lost (eliminated), drew (eliminated), missed the pick entirely (eliminated, no pick row ever created). See [decisions.md § LMS scoring and elimination](./decisions.md#lms-scoring-and-elimination).
- **Milestone 5, Slice 3 — LMS locking** — 2026-08-06, committed and pushed. `LmsEngine.lockEntries()` sets `lms_team_picks.locked_at` (`016_lms_team_picks_locked_at.sql`, applied) — not `game_entries.status`, a genuine divergence from Pick 5 since LMS entries are season-scoped and must stay `pending` across the whole competition. `validateEntry()` gained a live `gameweeks.deadline_utc` check. Found and fixed a real bug in the shared `compute-deadlines` function: its discovery step could never have found LMS entries needing locking, since it filtered on `game_entries.gameweek_id`, always null for LMS. Replaced with an unconditional "call every registered mode's `lockEntries()`" loop — simpler and genuinely mode-agnostic, not just `game_type`-branch-free. 7 new unit tests (140/140 total pass). Live-verified through the real `compute-deadlines` function (not a direct engine call): a pick submitted before deadline, then locked once its (test) gameweek's fixture moved into the past, then a further submission correctly rejected. See [decisions.md § LMS locking](./decisions.md#lms-locking).
- **Milestone 5, Slice 2 — LMS pick submission** — 2026-08-05, committed and pushed. `submit-lms-pick` Edge Function + `LmsEngine.validateEntry()` (first real LMS `GameEngine` method) + `lms_team_picks` (`015_lms_picks.sql`, applied). Unblocked by removing the LMS "cycle" concept entirely (`current_cycle` dropped, `014_lms_remove_cycle.sql`) — a team may never be picked twice across the whole competition, enforced by a real `unique` constraint. 17 new unit tests (133/133 total pass). Live-verified: valid pick, same-gameweek resubmission (upsert, not flagged as reuse), a changed-away team correctly rejected if picked again later, a team with no fixture in the gameweek rejected, an eliminated entry rejected. Found and worked around a second retired-prototype table-name collision (`lms_picks` → `lms_team_picks`). See [decisions.md § LMS: no cycles](./decisions.md#lms-no-cycles-current_cycle-removed-slice-2-implemented).
- **LMS architecture: Wipeout Resolution, automatic rollover, fixed entry fee, `rollover_generation`** — 2026-08-05, migration applied (not committed). `013_lms_wipeout_and_rollover.sql` (`pots.wipeout_resolution`/`season_end_tie_rule`/`start_gameweek_id`/`rollover_source_pot_id`/`carry_over_amount`/`rollover_generation`, `pot_prizes.rollover`) reviewed across correctness/replay safety/rollback/trigger compatibility/RLS compatibility/Pick 5 & Predictor compatibility, refined (added `rollover_generation` + a client-INSERT column-privilege fix closing a real fabricated-lineage risk), then applied via `supabase migration up --local`. See [decisions.md § LMS: multi-generation rollover review](./decisions.md#lms-multi-generation-rollover-review-found-a-real-gap-added-rollover_generation).
- **`ISSUE-32` — LMS entry-window gate** — 2026-08-05, implemented and verified live (not committed) → [current-state.md § Resolved issues](./current-state.md#resolved-issues). `checkEntryWindow()` in `get-or-create-lms-entry/validate.ts`, 6 new unit tests, 5 live scenarios verified through the real Edge Function. Found and fixed a bug in the verification script's own cleanup ordering, and a local-dev Kong/edge-runtime routing quirk (both documented in the ISSUE entry).
- **Milestone 5, Slice 1 — LMS entry creation** — 2026-08-05, committed. `get-or-create-lms-entry` Edge Function, mirroring `get-or-create-pick5-entry` exactly except for one structural difference: LMS entries are season-scoped (`entry_scope='season'`, `gameweek_id` null), matching the `game_entries_season_key` unique index built in Milestone 2 and never used until now. **No schema changes required** — `game_entries`/`game_entry_lms` already existed. No `LmsEngine` class yet (mirrors Pick 5's own Slice 1, where the Game Engine class first appeared in Slice 2). 4 new Deno unit tests (110/110 total pass). Verified live through the real Edge Function: entry creation, idempotency (second call returns the same entry, no duplicate row), wrong-`game_type` rejection, non-member rejection, missing-`pot_id` rejection. All test data removed by exact ID.
- **Payment Verification admin workflow** — `ISSUE-6`, 2026-08-05, committed and pushed → [current-state.md § Resolved issues](./current-state.md#resolved-issues). Admin page (`/admin/payments`, `pages/AdminPayments.jsx`) supporting both manual single-entry verification (paid/unpaid, reusing the already-built `PaymentTable.jsx`) and bulk CSV import (`Identifier,Pot,Status,Notes` — validate, preview, confirm, apply, with a processed/updated/skipped/failed summary and per-row outcome). Backend: one new `admin-actions` action, `bulk_verify_payments` (dry-run preview + apply, same code path either way), plus a pure, DB-free classification module (`bulkPayments.ts`, 18 unit tests). **No schema changes required** — see [decisions.md § Payment Verification bulk import](./decisions.md#payment-verification-bulk-import-no-schema-change-needed). Found and fixed a real bug: GoTrue stores phone numbers without a leading `+` — phone matching now strips it. Verified live, end-to-end, through the real application. See [session-log.md](./session-log.md).
- **Pick 5 production hardening sprint** — 2026-08-05, committed. Full production-readiness audit across database/RLS/indexes/Edge Functions/Game Engine/frontend/React Query/auth/realtime/notifications/admin actions/Payment Verification/documentation — see [current-state.md § Resolved issues](./current-state.md#resolved-issues) (`ISSUE-29`/`30`/`31`) and [decisions.md § Failure isolation](./decisions.md#failure-isolation-one-pots-gameweeks-error-must-never-block-anothers). Restored the `supabase_realtime` publication (was empty — every realtime subscription in the app was silently dead); dropped two undocumented, unintended RLS policies (`pots` DELETE, `leagues` `with check(true)` INSERT); added per-pot/per-gameweek failure isolation to `Pick5Engine.settle()` and `settle-gameweek/index.ts`; closed a TOCTOU race in `submit-pick5-picks`; added a debounce to `PotDetail.jsx`'s player search. Further findings (`ISSUE-26`/`27`/`28`) documented, deliberately not fixed. See [session-log.md](./session-log.md).
- **Pick 5 frontend cutover** — 2026-08-05, committed. Replaced every remaining frontend interaction with the retired prototype schema (`user_entries`/`user_entry_picks`/`leaderboard_snapshots`) with the Game Engine schema (`game_entries`/`pick5_picks`/`pot_standings_snapshots`) via `get-or-create-pick5-entry`/`submit-pick5-picks`. Migrated: `hooks/useEntry.js`, `hooks/useLiveScores.js`, `hooks/useLeaderboard.js`, `hooks/usePick5Entry.js` (fixed, wired in for the first time), `pages/PicksPage.jsx`, `pages/PotDetail.jsx` (the actual reachable pick-building flow), `pages/GameweekPage.jsx` (gained a new Standings section), `components/leaderboard/LeaderboardCard.jsx`/`LeaderboardTable.jsx`. Found and fixed `ISSUE-25` (`supabase.functions.invoke()` error messages were being swallowed) via a new shared `extractFunctionError()` helper. Verified live, end-to-end, through the real browser UI. See [session-log.md](./session-log.md).
- **Milestone 4, Slice 9 — Notifications (domain events)** — 2026-08-05, committed and pushed. `Pick5Engine.notifyUsers()` (the eighth and final real Game Engine contract implementation for Pick 5 — all eight `GameEngine` methods implemented). A pure domain-event emitter, inserting one row into `notifications`, called from within `awardPrize()` once per winner — see [decisions.md § Notifications: domain events, not delivery](./decisions.md#notifications-domain-events-not-delivery). 7 new Deno unit tests (59/59 in file, 87/87 total pass at the time). See [session-log.md](./session-log.md).
- **Pick 5 production readiness audit** — 2026-08-05, reviewed, approved, committed. Investigation-only (no code changed) — full findings across Game Engine, database, Edge Functions, migrations, documentation, security, open issues, and technical debt. Identified the frontend-cutover gap (below) as the top Critical finding. See [session-log.md](./session-log.md).
- **Milestone 4, Slice 8 — Prize awarding + prize pool deductions** — 2026-08-05, committed and pushed. `Pick5Engine.awardPrize()` (the seventh real Game Engine contract implementation) — wired into `settle()` alongside `determineWinner()` (Slice 7), so a single `settle-gameweek` invocation settles, ranks, and pays out in one pass. `pot_prizes` rows created lazily inside `awardPrize()`, keyed by real `id` (never a partial-index `upsert`, per the Slice 6 `pot_standings_snapshots` precedent). **Prize pool deductions** feature (Admin Fee, Charity Fee — see [decisions.md § Prize pool deductions](./decisions.md#prize-pool-deductions-admin-fee-and-charity-fee)) applied via migration `010_prize_pool_deductions.sql`. The Game Engine only ever distributes `net_amount`, never `gross_amount`. Resolves no `ISSUE-N` (net-new Game Engine build-out plus a net-new product feature). 11 new Deno unit tests (53/53 in file, 81/81 total pass at the time). Verified live end-to-end. See [session-log.md](./session-log.md).
- **Milestone 4, Slice 7 — Winner determination** — 2026-08-05, committed and pushed. `Pick5Engine.determineWinner()` (the sixth real Game Engine contract implementation) — identifies the rank-1 user(s) of a pot's most recently settled gameweek, correctly returning multiple winners on a tie (Slice 6's shared-rank rule). Implemented standalone at the time; wired into `settle()` in Slice 8 alongside `awardPrize()`. Resolves no `ISSUE-N` (net-new Game Engine build-out). 6 new Deno unit tests (71/71 total pass at the time). Verified live against the real database. See [session-log.md](./session-log.md).
- **Milestone 4, Slice 6 — Standings** — 2026-08-05, committed. `Pick5Engine.generateStandings()`, called internally from `settle()`. Resolved `ISSUE-15`/`ISSUE-17`. See [session-log.md](./session-log.md).
- **Milestone 4, Slice 5 — Settlement** — 2026-08-05, committed. `Pick5Engine.settle()` wired into `settle-gameweek` via the dispatcher — implements the payment-void rule deferred from Slice 4. See [session-log.md](./session-log.md).
- **Milestone 4, Slices 3-4 — Locking and scoring** — 2026-08-05, committed (`be06bbd`). `Pick5Engine.lockEntries()`/`calculateScore()` wired into `compute-deadlines`/`compute-scores`. See [session-log.md](./session-log.md).
- **Milestone 4, Slice 2 — Pick submission** — 2026-08-04. `submit-pick5-picks` Edge Function + `Pick5Engine.validateEntry()` (the first real Game Engine contract implementation) + `pick5_picks` table (`007`) + `available_players_by_gameweek` fix (`008`, `ISSUE-23`). See [session-log.md](./session-log.md).
- **ISSUE-22 resolved — Edge Runtime JWT verification fixed by CLI/Edge Runtime upgrade** — 2026-08-04. CLI `2.75.0`→`2.111.0`, Edge Runtime `v1.70.0`→`v1.74.2`. Re-verified from scratch with a brand-new user via real `supabase.functions.invoke()` calls: `admin-actions` now `403` (correctly authorized-but-denied, not rejected), `get-or-create-pick5-entry` now genuine `200` success. See [current-state.md](./current-state.md#issue-22--edge-runtimes-default-jwt-verification-rejected-gotrues-es256-tokens-no-authenticated-edge-function-call-worked-locally) and [session-log.md](./session-log.md).
- **Milestone 4, Slice 1 — Pick 5 entry creation** — 2026-08-03. `get-or-create-pick5-entry` Edge Function, `hooks/usePick5Entry.js`. Database-layer logic verified directly at creation time; HTTP/auth-layer end-to-end path now also confirmed live 2026-08-04 following the `ISSUE-22` fix above. See [session-log.md](./session-log.md).
- **Shared platform schema deployed: `004`/`005` applied, ISSUE-21 resolved for the 2 blocking objects, ISSUE-20 narrowed to only the un-migrated prototype tables, `006` (cron header fix) applied** — 2026-08-03. See [session-log.md](./session-log.md#2026-08-03-7--track-b-resolved-shared-platform-schema-deployed). Fully verified live: 7 tables, 14 FKs, 21 indexes, 5 triggers, 10 RLS policies, zero data loss on any existing table.
- **Milestones 1–3: three-game-mode platform architecture, shared schema design, Game Engine framework** — 2026-08-03. No single `ISSUE-N` (this is the strategic rebuild superseding the retired LMS/Predictor prototype). See [game-engine.md](./game-engine.md), [schema-review.md](./schema-review.md), [session-log.md](./session-log.md#2026-08-03-5--live-evidence-priority-review-drift-investigation-and-the-start-of-a-three-game-mode-platform-rebuild). Framework verified by the repo owner running the Deno test suite locally.
- **Live-evidence drift investigation: `/drift` command created; ISSUE-19/20/21 discovered and logged; ISSUE-1/2/3/4 verified against the live database** — 2026-08-03. See [session-log.md](./session-log.md#2026-08-03-5--live-evidence-priority-review-drift-investigation-and-the-start-of-a-three-game-mode-platform-rebuild).
- **Repository hygiene remediation: secrets and Chrome-profile data removed from git tracking, comprehensive `.gitignore` added, `.env.example` created** — `ISSUE-5`, `ISSUE-14` (both P0/P2) → [current-state.md § Resolved issues](./current-state.md#resolved-issues). 2026-08-03. See [session-log.md](./session-log.md).
- **Documentation restructure: issue register, cross-references, doc ownership map** — 2026-08-03. No `ISSUE-N` (this was the documentation work that created the issue-tracking system itself). See [session-log.md](./session-log.md#2026-08-03-2--documentation-restructure-remove-duplication-add-cross-references).
- **Initial documentation pass: `docs/` created from a full codebase audit** — 2026-08-03. No `ISSUE-N`. See [session-log.md](./session-log.md#2026-08-03-1--initial-documentation-pass).
- **Project management layer: `project-board.md`, `business-rules.md`, `engineering-principles.md` created; `/checkpoint` upgraded; `/preflight` added; `ISSUE-17` and `ISSUE-18` discovered and logged** — 2026-08-03. No single `ISSUE-N` (infrastructure work). See [session-log.md](./session-log.md) for this session's entry.
